On evUserChangedPage
	If $cinst.$objs.pagedPaneArticles.$currentpage=1
		Do $cinst.$objs.pagedPaneArticles.$currentpage.$assign(2)
		Do method $showDetails
	Else
		Do $cinst.$objs.pagedPaneArticles.$currentpage.$assign(1)
	End If